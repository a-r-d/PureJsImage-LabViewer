# Shared TypeScript configuration

`base.json` is DOM-free and strict. Browser and Node projects extend the matching adapter so
cross-runtime packages do not acquire ambient platform types accidentally.
