# Playwright configuration

The root `playwright.config.ts` owns browser projects and the preview server. Product tests live
under `apps-e2e/workbench/tests` so browser workflow code remains outside the application.
